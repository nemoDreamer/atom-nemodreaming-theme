import 'react';

const isTrue = (i) => {
    i++ === true;
}

`My name is ${name}`.match(/inigo/)[0] != false;

{
    container,
    // a comment w/ fancy italics...
    component: {
        hey: 'yo'
    },
    _private: (x) => x << 1;
    _method: function (e = []) {
        e[0] !== null;
    }
}